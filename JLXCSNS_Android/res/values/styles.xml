<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- 自定查看大图加载中的dialog的样式 -->
    <style name="loading_image_dialog" parent="android:style/Theme.Dialog">

        <!-- 背景颜色及透明程度 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 是否有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否模糊 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 是否有边框 -->
        <item name="android:windowFrame">@null</item>
    </style>

    <!-- 自定item的dialog的样式 -->
    <style name="item_dialog" parent="android:style/Theme.Dialog">

        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@color/transparent</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!-- 自定alertDialog的样式 -->
    <style name="alert_dialog" parent="android:style/Theme.Dialog">

        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@color/transparent</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    <!-- 自定progressDialog的样式 -->
    <style name="progess_dialog" parent="android:style/Theme.Dialog">
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:background">@drawable/progress_dialog_shape</item>
        <!-- dialog背景样式 -->
        <item name="android:windowBackground">@color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 模糊 -->
    </style>

    <!-- 相册部分 弹出框动画 -->
    <style name="anim_popup_dir">
        <item name="android:windowEnterAnimation">@anim/gallery_slide_in</item>
        <item name="android:windowExitAnimation">@anim/gallery_slide_out</item>
    </style>

    <!-- 话题频道菜单弹出动画 -->
    <style name="anim_group_menu">
        <item name="android:windowEnterAnimation">@anim/group_menu_fade_in</item>
        <item name="android:windowExitAnimation">@anim/group_menu_fade_out</item>
    </style>
    
     <!-- 话题操作菜单弹出动画 -->
    <style name="anim_group_operate">
        <item name="android:windowEnterAnimation">@anim/group_operate_fade_in</item>
        <item name="android:windowExitAnimation">@anim/group_operate_fade_out</item>
    </style>

</resources>